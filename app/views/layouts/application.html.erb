<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> | Reel - Your Number One Fishing Destination Finder</title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>
  <body class='<%= controller.controller_name %>'>
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
        <!-- Since we don’t want to display debug information to users of a deployed
        application we use "if Rails.env.development?" This is one of three
        environments defined by default in Rails. In particular,
        Rails.env.development? is true only in a development environ.  -->
    </div>
  </body>
</html>
